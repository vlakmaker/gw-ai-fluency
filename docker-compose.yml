# Instance 1: playbook.informationgeek.org
# Caddy reverse-proxies to localhost:8080
services:
  playbook:
    build: .
    container_name: gw-ai-fluency-playbook
    restart: unless-stopped
    ports:
      - "8080:80"
